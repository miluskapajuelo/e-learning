import React from 'react';
import './Menu.scss';
import CardCourse from '../CardCourse/CardCourse';

const Menu = () => (

  <section className="Menu">
    <article className="Menu__image">
      <p>holi menu</p>
      <CardCourse />
    </article>
  </section>
);

export default Menu;
